/**
 * Prisma client wrapper for main-backend
 * Re-exports the shared Prisma client from @fundifyhub/prisma package
 */
import { prisma } from '@fundifyhub/prisma';

export default prisma;
export { prisma };
