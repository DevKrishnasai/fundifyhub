/**
 * Geography-related constants
 * Aligned with Prisma schema
 */

// No enums in schema for geography, but we can add useful constants

export const DEFAULT_COUNTRY_CODE = 'IN';
export const DEFAULT_COUNTRY_DIAL = '+91';
